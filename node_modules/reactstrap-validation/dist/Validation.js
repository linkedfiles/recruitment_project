"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
function validate(state) {
    var valid = state.ref && state.ref.validity.valid;
    var validationMessage = state.ref && !valid && state.ref.validationMessage;
    return __assign({}, state, { valid: valid, validationMessage: validationMessage });
}
exports.validate = validate;
function forceValidate(state) {
    state.ref.blur();
    return validate(state);
}
exports.forceValidate = forceValidate;
