import * as React from 'react';
import { IState, IValidatingFormGroupProps } from './Types';
export declare class ValidatingFormGroup extends React.Component<IValidatingFormGroupProps, IState> {
    constructor(props: any);
    componentWillReceiveProps(nextProps: IValidatingFormGroupProps): void;
    private enhance(element, index);
    private reference(ref);
    private isValid();
    private handle(innerHandle, trigger);
    render(): any;
}
